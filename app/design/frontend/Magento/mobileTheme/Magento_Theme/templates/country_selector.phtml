<?php
// Path to the flags folder
$flagsPath = $this->getViewFileUrl('images/flags');

// Array of countries with their respective flags (assuming you have this data)
$countries = [
    'EN' => 'United Kingdom',
    'US' => 'United States',
    'CA' => 'Canada',
    // Add more countries as needed
];
?>

<div class="flag-and-select-container">
    <!-- Display Flag -->
    <div class="flag-container">
        <?php foreach ($countries as $countryCode => $countryName): ?>
            <img id="<?= $countryCode ?>-flag" class="flag" src="<?= $flagsPath . '/' . $countryCode . '.svg' ?>" alt="<?= $countryCode ?> Flag" style="display: none;">
        <?php endforeach; ?>
    </div>

    <!-- Display Select Element -->
    <div class="select-container">
        <select id="country-select" name="country">
            <?php foreach ($countries as $countryCode => $countryName): ?>
                <option value="<?= $countryCode ?>" data-flag="<?= $countryCode ?>" <?= $countryCode == 'EN' ? 'selected' : '' ?>>
                    <?= $countryCode ?>
                </option>
            <?php endforeach; ?>
        </select>
    </div>
</div>

<button class="navbar-toggler" type="button">
    <div class="hamburger" onclick="toggleMenu()">
        <span></span>
        <span></span>
        <span></span>
    </div>
</button>